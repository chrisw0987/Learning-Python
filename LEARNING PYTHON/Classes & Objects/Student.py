class Student:

    def __init__(self, name, major, gpa, is_on_probation):
        self.name = name                                   #the name of the object is what the user passes in
        self.major = major                                 #the major of the object is what the user passes in
        self.gpa = gpa                                     #the major of the object is what the user passes in
        self.is_on_probation = is_on_probation             #the major of the object is what the user passes in